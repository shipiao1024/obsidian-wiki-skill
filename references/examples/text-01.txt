这是一段用于主入口回归测试的标准纯文本样本。它的目标不是提供复杂知识，而是验证 `--text` 路径是否还能稳定进入统一入口，最终生成 raw、source 和 brief 三层产物。

这段文本刻意保持中等长度，既不短到失去测试意义，也不长到影响日常回归速度。它应该能够触发正常的标题推断、正文写入、索引更新和日志追加。

如果未来入口重构、adapter 分层、quality 透传、review queue 或 wiki_lint 的行为再次发生变化，这段纯文本样本可以作为最稳定的最小基线之一，用来排除网络、网页 adapter、视频字幕和 PDF 提取器带来的外部变量。
